<% @page_heading = _('New article') %>

<%= render "admin/shared/edit", { :form_type => "article", :form_action => { :action => "new", :id => @article.id , :class => ('autosave')} } %>

<%= form_tag :action => "merge", :method => :put, :id => @article.id do %>

  <%= render :partial => "merge_articles" if current_user.admin? && @article && @article.id %>

<% end %>